port: 7880
rtc:
  udp_port: 7882
  tcp_port: 7881
  use_external_ip: true
turn:
  enabled: false
logging:
  level: info
  json: false
# API keys are NOT stored here — supplied at runtime via the LIVEKIT_KEYS
# environment variable (set as a Fly secret in Task 10).
